########################################
# 仓库结构说明
#
# 本仓库为统一的 Mod 管理仓库（DolocMods），
# 其结构如下：
#
# DolocMods/                      ← 主 Git 仓库（推送到 GitHub）
# ├── ModSettingManager/         ← 主展示项目，完整托管
# ├── EasyFishing_DolocTown/     ← 子项目，仅本地 Git 管理（主仓库忽略其 .git）
# ├── OtherModX/                 ← 其他 Mod 项目，亦可保留本地 Git
# └── .gitignore                 ← 当前忽略配置文件
#
# 每个子项目可以拥有独立的 Git 仓库用于本地提交，
# 但主仓库不会跟踪它们的 .git，以避免子模块、嵌套仓库冲突。
#
# 推荐在主仓库层级统一 push 至 GitHub，仅保留一个远程仓库。
########################################

########################################
# Visual Studio 本地工作区相关文件
########################################
.vs/
*.user
*.suo
*.sln.docstates

########################################
# 构建输出（通用于 .NET / Unity 项目）
########################################
bin/
obj/
*.cache

########################################
# 操作系统临时文件
########################################
# macOS
.DS_Store

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

########################################
# IDE 设置目录（防止子文件夹中的 IDE 文件污染主仓库）
########################################
**/.idea/
**/.vscode/

########################################
# 忽略嵌套子项目的本地 Git 仓库
# 允许它们本地使用 git，但主仓库不跟踪
########################################
**/.git